import { Meta, ColorPalette, ColorItem, Source } from '@storybook/blocks';
import { colors } from '.';

<Meta title="Colors/Colors" />

# Colors

## 사용 방법

<Source
  language="js"
  dark
  code={`import { black, blue400 } from 'cocorig-ui';`}
/>

## 색상

export function colorItemList(items) {
  return Object.entries(items).map(([key, color]) => {
    if (typeof color === 'object') {
      return <ColorItem title={key} subtitle="" colors={color} />;
    }
    if (typeof color === 'string') {
      return <ColorItem title={key} subtitle="" colors={{ [key]: color }} />;
    }
  });
}

<ColorPalette>{colorItemList(colors)}</ColorPalette>
